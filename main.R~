source("MLE.R")

numUsed = 600	# Number of training images to be used

# Retrieve labels and training data & sort by the specified number of test images used
labels = getLabels()
traindata = getImageData()
sortedData = sortByDigit(traindata, labels, numUsed)

# Determine the mean & sd for each of the number sets using the respective
# data as well as the calculated components.

for(i in seq(1, 10)){
	results = calcNumberDist(numData)
}


testdata = getImageData()

